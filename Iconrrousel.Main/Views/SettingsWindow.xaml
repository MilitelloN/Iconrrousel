<Window x:Class="Iconrrousel.Main.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Iconrrousel.Main"
        mc:Ignorable="d"
        Title="Settings" Height="550" Width="336"
        Background="{Binding BorderBackground, Source={x:Static local:App.Data}}">
    <Window.Resources>
        <Style x:Key="SettingsButton" TargetType="Button">
            <Setter Property="Background" Value="{Binding TopButtonBackground, Source={x:Static local:App.Data}}"/>
            <Setter Property="Foreground" Value="{Binding TopButtonForeground, Source={x:Static local:App.Data}}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="10,5" BorderThickness="0" BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{Binding TopButtonHover, Source={x:Static local:App.Data}}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{Binding TopButtonPressed, Source={x:Static local:App.Data}}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="{Binding TopButtonBackground, Source={x:Static local:App.Data}}"/>
                                <Setter Property="Foreground" Value="#888" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Border x:Name="Bg" Background="{Binding TopButtonBackground, Source={x:Static local:App.Data}}" CornerRadius="8" BorderBrush="{Binding TopButtonHover, Source={x:Static local:App.Data}}" BorderThickness="0" Padding="8,5" SnapsToDevicePixels="True">
                <Grid>
                    <ContentPresenter Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    <Path x:Name="Arrow" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0" Fill="{Binding TopButtonForeground, Source={x:Static local:App.Data}}" Data="M 0 0 L 4 4 L 8 0 Z" />
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Bg" Property="Background" Value="{Binding TopButtonHover, Source={x:Static local:App.Data}}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="Bg" Property="Background" Value="{Binding TopButtonPressed, Source={x:Static local:App.Data}}" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter TargetName="Arrow" Property="Fill" Value="#888" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="ThemedComboBox" TargetType="ComboBox">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Background" Value="{Binding TopButtonBackground, Source={x:Static local:App.Data}}"/>
            <Setter Property="Foreground" Value="{Binding TopButtonForeground, Source={x:Static local:App.Data}}"/>
            <Setter Property="BorderBrush" Value="{Binding TopButtonHover, Source={x:Static local:App.Data}}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Template="{StaticResource ComboBoxToggleButton}"
                                          Grid.Column="2"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"/>
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="5,3,29,3"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              RecognizesAccessKey="True"/>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="False"
                                    Focusable="False"
                                    PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="{Binding BorderBackground, Source={x:Static local:App.Data}}" BorderBrush="{Binding TopButtonHover, Source={x:Static local:App.Data}}" BorderThickness="0"/>
                                    <ScrollViewer Margin="5,7,5,7" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="114" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#888" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{Binding BorderBackground, Source={x:Static local:App.Data}}" Padding="14" CornerRadius="10" BorderThickness="1">
        <ScrollViewer VerticalScrollBarVisibility="Hidden">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <GroupBox Header="General" Grid.Row="0" Margin="0,0,0,7" MaxWidth="300" Foreground="{Binding IconTextForeground, Source={x:Static local:App.Data}}" Background="Transparent">
                    <StackPanel>
                        <CheckBox Content="Start with windows" x:Name="StartUpCbox" Margin="0,4" Foreground="{Binding IconTextForeground, Source={x:Static local:App.Data}}" />
                        <CheckBox Content="Display names" x:Name="NamesCbox" Margin="0,4" Foreground="{Binding IconTextForeground, Source={x:Static local:App.Data}}" />
                        <ComboBox x:Name="ThemeCombo" Width="180" Margin="0,4" HorizontalAlignment="Left" SelectedIndex="0"
                                  Style="{StaticResource ThemedComboBox}"
                                  Background="{Binding TopButtonBackground, Source={x:Static local:App.Data}}"
                                  Foreground="{Binding TopButtonForeground, Source={x:Static local:App.Data}}"
                                  BorderBrush="{Binding TopButtonHover, Source={x:Static local:App.Data}}" SelectionChanged="ThemeCombo_SelectionChanged">
                            <ComboBox.ItemContainerStyle>
                                <Style TargetType="ComboBoxItem">
                                    <Setter Property="Background" Value="{Binding TopButtonBackground, Source={x:Static local:App.Data}}" />
                                    <Setter Property="Foreground" Value="{Binding ComboItemForeground, Source={x:Static local:App.Data}}" />
                                    <Setter Property="Padding" Value="7,5" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ComboBoxItem">
                                                <Border x:Name="itemBorder" Background="{TemplateBinding Background}" Padding="7,5" CornerRadius="5">
                                                    <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsHighlighted" Value="True">
                                                        <Setter TargetName="itemBorder" Property="Background" Value="{Binding TopButtonHover, Source={x:Static local:App.Data}}" />
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="itemBorder" Property="Background" Value="{Binding TopButtonPressed, Source={x:Static local:App.Data}}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ComboBox.ItemContainerStyle>
                            <ComboBoxItem Content="Dark theme" />
                            <ComboBoxItem Content="Light theme" />
                            <ComboBoxItem Content="Beige theme" />
                            <ComboBoxItem Content="Blue theme" />
                            <ComboBoxItem Content="Pink theme" />
                            <ComboBoxItem Content="Red theme" />
                        </ComboBox>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Window size" Grid.Row="1" Margin="0,0,0,7" MaxWidth="300" Foreground="{Binding IconTextForeground, Source={x:Static local:App.Data}}" Background="Transparent">
                    <StackPanel>
                        <TextBlock Text="Choose the size of the program" Margin="0,0,0,7" TextWrapping="Wrap" Foreground="{Binding IconTextForeground, Source={x:Static local:App.Data}}" />
                        <StackPanel x:Name="RadioStack" Orientation="Horizontal" HorizontalAlignment="Left">
                            <RadioButton Content="Small" GroupName="IconSizeGrp" Margin="0,0,10,0" Tag="Small" Foreground="{Binding IconTextForeground, Source={x:Static local:App.Data}}" />
                            <RadioButton Content="Medium" GroupName="IconSizeGrp" Margin="0,0,10,0" Tag="Medium" IsChecked="True" Foreground="{Binding IconTextForeground, Source={x:Static local:App.Data}}" />
                            <RadioButton Content="Large" GroupName="IconSizeGrp" Margin="0,0,10,0" Tag="Large" Foreground="{Binding IconTextForeground, Source={x:Static local:App.Data}}" />
                            <RadioButton Content="Extra Large" GroupName="IconSizeGrp" Tag="ExtraLarge" Foreground="{Binding IconTextForeground, Source={x:Static local:App.Data}}" />
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Blocking time ranges" Grid.Row="2" Margin="0,0,0,7" MaxWidth="300" Foreground="{Binding IconTextForeground, Source={x:Static local:App.Data}}" Background="Transparent">
                    <StackPanel>
                        <ListBox x:Name="TimeRangesListBox" Height="120" Margin="3,3,3,7" Background="{Binding TopButtonBackground, Source={x:Static local:App.Data}}" Foreground="{Binding IconTextForeground, Source={x:Static local:App.Data}}" BorderThickness="0"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Content="Add Time Range" Width="120" Height="28" Click="AddTimeRange_Click" Margin="0,0,7,3" Style="{StaticResource SettingsButton}"/>
                            <Button Content="Remove Selected" Width="120" Height="28" Click="RemoveTimeRange_Click" Margin="0,0,7,3" Style="{StaticResource SettingsButton}"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <StackPanel Grid.Row="3" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,5,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,7">
                        <Button Content="Delete all icons" Width="120" Height="34" Click="DeleteAllIcons_Click" Margin="0,0,7,0" Style="{StaticResource SettingsButton}" />
                        <Button Content="Save" Width="120" Height="34" Click="CloseWin_Click" Style="{StaticResource SettingsButton}" />
                    </StackPanel>
                    <Button Content="Close application" Width="168" Height="34" Click="CloseApp_Click" Style="{StaticResource SettingsButton}" />
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Border>
</Window>
