<Window
    x:Class="Iconrrousel.Main.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Iconrrousel.Main"
    xmlns:conv="clr-namespace:Iconrrousel.Main.Converters"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Iconroussel"
    Height="190"
    MinWidth="320"
    SizeToContent="Width"
    ResizeMode="NoResize"
    Loaded="Window_Loaded"
    WindowStartupLocation="Manual"
    AllowDrop="True"
    DragEnter="Grid_DragEnter"
    Drop="Grid_Drop"
    AllowsTransparency="True"
    WindowStyle="None"
    Background="Transparent"
    mc:Ignorable="d">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <conv:ShowNamesToSizeConverter x:Key="ShowNamesToSize" />
        <Style x:Key="ModernScrollButton" TargetType="Button">
            <Setter Property="Background" Value="#1AFFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="12"
                                Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#38FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#52FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TopButton" TargetType="Button">
            <Setter Property="Background" Value="#30FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="8,3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#50FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#70FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border Background="#E0000000" 
            CornerRadius="15" 
            Margin="10"
            Padding="6">
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                            Direction="270" 
                            ShadowDepth="5" 
                            BlurRadius="20" 
                            Opacity="0.7"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" MinHeight="120" />
            </Grid.RowDefinitions>

            <Button
                x:Name="OpenConfigButton"
                Grid.Row="0"
                Content="⚙"
                Style="{StaticResource TopButton}"
                Width="35"
                Height="26"
                Margin="0,0,0,6"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Click="OpenWindow_Click"/>

            <Grid Grid.Row="1" Margin="5,0,5,0">
                <!-- Scroll content -->
                <ScrollViewer x:Name="IconViewer" 
                              MinWidth="50"
                              MaxWidth="600"
                              Margin="0"
                              Padding="46,6,46,10"
                              HorizontalScrollBarVisibility="Hidden" 
                              VerticalScrollBarVisibility="Disabled"
                              Background="Transparent"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Center">
                    <WrapPanel x:Name="IconsPanel" 
                               Orientation="Horizontal" 
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center" />
                </ScrollViewer>

                <!-- Overlayed buttons -->
                <Button
                    x:Name="ScrollLeft"
                    Content="◀" 
                    Style="{StaticResource ModernScrollButton}"
                    ClickMode="Press" 
                    Click="ScrollLeft_Click"
                    Width="38" 
                    Height="72"
                    Margin="4,0,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Panel.ZIndex="1"/>

                <Button
                    x:Name="ScrollRight" 
                    Content="▶"
                    Style="{StaticResource ModernScrollButton}"
                    ClickMode="Press"
                    Click="ScrollRight_Click"
                    Width="38"
                    Height="72"
                    Margin="0,0,4,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Panel.ZIndex="1"/>
            </Grid>
        </Grid>
    </Border>
</Window>
